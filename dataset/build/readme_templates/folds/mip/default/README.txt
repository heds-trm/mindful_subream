This folder contains the default folds for all existing modes (ie. `b_g_m`, `b_m`, ...).
These folds were randomly generated, with two contraints:
Different images from a single patient are in the same partitions.
The selected configuration is the one for which the prevalence of each label across partitions is the closest.