This is the root folder of the last build made for the Subream dataset at the following date: {datestamp}.

In this folder, you will find:
- The "images" folder, which contains image data (with various versions).
- The "folds" folder, which contains pre-computed train/validation/test folds (for various versions of data, labels and lymphnode prevalence)
- The "points" folder, which contains copies of all valid points found in the original data directory.